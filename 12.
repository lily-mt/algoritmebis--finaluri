12.აღწერეთ აბსტრაქტულ მონაცემთა ტიპი BST(ორობითი ძებნის ხე)და მიუთითეთ მასზე განმარტებული ოპერაციები.

BST(Binary Search Tree)არის მონაცემთა სტრუქტურა,სადაც:
     თითოეულ წვეროს გააჩნია ორი შვილი, მარჯვენა და მარცხენა შვილი. მარცხენა შვილის მნიშვნელობა ნაკლებია მის მშობელზე,ხოლო  მარჯვენა შვილის  მნიშვნელობა მეტია მის მშობელზე.
     მარცხენა ნახევარ ხეში ჩალაგებულია  root-  ზე ნაკლები მნიშვნელობის ელემენტები, ხოლო მარჯვენაში მასზე მეტი.

  ძირითადი ოპერაციები:
    1. insert(data)-ელემენტის დამატება
    2. delete(data)-ელემებტის წაშლა
    3. search(data)-ელემენტის ძიება
       სამივე ფუნქციის დროის სირთულეა O(log n).
