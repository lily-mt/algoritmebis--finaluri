დაწერეთ არაორიენტირებული გრაფოს აგების პროცედურა addEdge(int v, int w)    მეზობელი წვეროების ვექტორებით აღწერის ინსტრუქციებით.


void addEdge(vector<int>adj[], int v, int w){
  adj[v].push_back(w);
  adj[w].push_back(v);
  }


