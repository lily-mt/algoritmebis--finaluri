დაწერეთ ძებნის ორობით ხეში მინიმუმის პოვნის ფსევდოკოდი.

node* search_min(node* root){
    if(root == NULL) return NULL;
    while (root->left != NULL){
        root = root->left;
    }
    return root;
}
