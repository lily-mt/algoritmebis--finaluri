18.დაწერეთ გრაფის წვეროების ინიციალიზაციის ინსტრუქცია ბმული სიების გამოყენებით.


struct graph {
    int V;
    node** adj;   

    graph(int V) {
        this->V = V;
        adj = new node*[V];
        for (int i = 0; i < V; i++) \
            adj[i] = NULL;  
    }
};
